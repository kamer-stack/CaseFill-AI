# Runtime uploads (regenerated per session)
uploads/

# Dependencies (installed via npm install / pip install)
node_modules/
__pycache__/
*.pyc

# Local-only env overrides
.env

# OS junk
.DS_Store
Thumbs.db
